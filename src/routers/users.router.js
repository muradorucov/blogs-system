const express = require("express");
const Router = express.Router();

Router.get("/", () => { })
Router.post("/", () => { })
Router.put("/:id", () => { })
Router.patch("/:id", () => { })
Router.delete("/:id", () => { })
Router.get("/:id", () => { })


module.exports = Router;